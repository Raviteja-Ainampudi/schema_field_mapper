[pytest]
# pythonpath keeps a bare `pytest` working without setting PYTHONPATH first,
# which matters because the package lives under src/.
pythonpath = src
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:schema_mapper.*
